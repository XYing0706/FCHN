# FCHN_20260825 新会话交接文档

更新时间：2026-08-31（Asia/Shanghai）  
适用对象：对此前会话完全没有上下文、需要继续本项目实施的新会话  
项目唯一根目录：`C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825`

> 本文区分三类状态：**已确认完成**、**已确认未完成**、**必须到服务器现场核验**。不得把“代码测试通过”“单个 pilot 通过”“日志无报错”误写成整个阶段完成。

## 1. 一页结论：现在做到哪里

本项目正在建立并执行一个面向多中心静息态功能连接数据的 FCHN（FC + HOFC + normative healthy-anchor + strict late fusion）完整、可审计、可恢复实验流程。正式执行分为五个阶段：

| 阶段 | 任务 | 当前结论 |
|---|---|---|
| A | 权威脑图谱元数据、稳定 ROI/edge 身份、跨数据集共同空间、subjects、QC、20 个最终输入单元 | **已完成并通过门禁** |
| B | 4 atlas × 4 disease dataset × 2 protocol 的 split plan 与 strict nested lineage | **已完成并通过门禁** |
| C | 428 个 outer fold 的 FC/HOFC/normative/Meta/Platt 完整闭环 | **方法、代码和 3 类真实实体门禁已通过；全量 428 folds 未确认完成** |
| D | 全部 outer folds 完成后的指标、校准、异常评分和 biomarker 汇总 | **尚未实现；严禁提前启动** |
| E | 全 run 身份、拟合边界、hash、schema、有限性审计与发布 | **尚未实现** |

本机 Stage C 已按用户要求终止，不再监控，也不得重启。最后核验为 `15/428` 个本机 fold marker、`staging=0`、无阶段级 manifest、无阶段级 `_SUCCESS`。这些本机 fold 不能与服务器结果混合。

服务器已经完成 Stage A/B，并通过 Stage C 三个真实实体门禁：

1. `HO112/adhd/LOSO/outer000`：常规完整 fold；
2. `HO112/adhd/LOSO/outer003`：缺失协变量保留身份并标记 ineligible；
3. `HO112/abide/LOSO/outer000`：ABIDE 7 位前导零身份端到端保持。

用户报告实体门禁结果为 `3 passed`。但是，现有会话中**没有用户明确确认服务器的 428-fold 并行 launcher 已经正式启动**。因此新会话的第一件事是登录服务器核验进程、PID、fold 数和日志；不能假定它正在运行，也不能贸然重复启动。

## 2. 新会话接手后的第一组动作

先在本机只读以下文件，本文是入口，其他文件是细节依据：

```text
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\doc\HANDOFF20260831.md
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\doc\FCHN_HANDOFF_MANUAL_20260825.md
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\doc\PHASE_A_IMPLEMENTATION_20260825.md
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\doc\PHASE_B_IMPLEMENTATION_20260826.md
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\doc\PHASE_C_IMPLEMENTATION_20260826.md
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\task_plan.md
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\findings.md
C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825\progress.md
```

然后登录服务器，只做现场核验，不先启动第二个进程：

```bash
PROJ=/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825
cd "$PROJ"

cat outputs/stage_c/server_driver.pid 2>/dev/null || true
pgrep -af '03_run_stage_c.py|run_stage_c_parallel_server.sh' || true
find outputs/stage_c/folds -type f -name _SUCCESS 2>/dev/null | wc -l
find outputs/stage_c/folds -type d -name '*.staging' 2>/dev/null | wc -l
find outputs/stage_c/logs -type f -name '*.err' -size +0 2>/dev/null | wc -l
grep -RIl 'Traceback' outputs/stage_c/logs 2>/dev/null | wc -l
test -f outputs/stage_c/stage_c_manifest.json && echo manifest_present || echo manifest_absent
test -f outputs/stage_c/_SUCCESS && cat outputs/stage_c/_SUCCESS || echo success_absent
tail -n 30 outputs/stage_c/server_driver_stdout.log 2>/dev/null || true
tail -n 80 outputs/stage_c/server_driver_stderr.log 2>/dev/null || true
```

判读规则：

- `pgrep` 有 Stage C 进程、fold 数持续增加、错误日志无 traceback：继续等待，不重复启动。
- 没有进程且 fold 数小于 428：先完整读取所有非空 `.err` 和 driver stderr；有 traceback 就按“完整 traceback → 聚合比较 → TDD 失败测试 → 最小修复 → 真实失败 fold 回读”的顺序处理。
- 没有进程、无 traceback、并行 launcher 从未启动：按本文第 12 节启动。
- `428` 个 marker 只是初筛；最终必须由一次单进程 `scripts/03_run_stage_c.py --all` 对全部 fold 做 exact-fresh 核验并发布 manifest 与 `_SUCCESS`。
- 只有 `stage_c_manifest.json.status=passed`、expected/ready 都是 428、所有 run/artifact hash 新鲜且 `_SUCCESS` 内容为 `stage_c_ready`，Stage C 才完成。

注意：`PHASE_C_IMPLEMENTATION_20260826.md` 的方法、字段和产物合同仍是细节依据，但其末尾的 PID 和运行进度停留在旧时点；**当前运行状态以本文和服务器现场核验为准**。

## 3. 不可违反的项目边界

1. 所有新增文件只能写入：

   ```text
   C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825
   ```

2. 以下目录只读借鉴，不得覆盖：

   ```text
   C:\XY\FedTopo-Brain\CHyCR_20260621
   C:\XY\FedTopo-Brain\SSL\data
   ```

3. 单个 `dataset × atlas` 是最小 FC 加载和存储单元。严禁把一个数据集的全部 atlas 合并，也严禁把一个 atlas 的全部数据集合并成巨大 FC 文件。
4. 删除或判定无效 ROI 后，必须保留其 atlas-local 原始编号、全局原始列、结构状态和 edge 两端点。严禁按删后位置解释 ROI/edge。
5. 不得猜测 atlas 名称、description、人口学语义或编码。没有权威依据的字段留空并记录限制。
6. 不得输出被试级身份列表、site/age/sex/mean FD 或完整 phenotype 行。日志和测试只输出聚合计数、shape、字段、任务身份和 hash。
7. 阶段 C 期间不得改变 Stage A/B、QC、split 分配、C 网格、KMeans、protocol、缺失协变量策略或 biomarker 定义。
8. 任何 runner/config/Stage B hash 变化都会使旧 fold stale。旧结果必须移动到 `outputs/archive/` 保留证据，不得删除后假装首次运行。
9. 项目不是 Git 仓库；版本和恢复边界依赖 source/config/artifact SHA-256、manifest、staging 和 `_SUCCESS`。

## 4. 输入、项目和服务器目录

### 4.1 本机

```text
正式项目：C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825
父项目参考：C:\XY\FedTopo-Brain\CHyCR_20260621
原始数据：C:\XY\FedTopo-Brain\SSL\data
```

原始数据包含 5 个 dataset：`adhd, abide, abide2, mdd, fcp`；4 个 atlas：`HO112, AAL116, Dosenbach160, CC200`。源 FC NPZ 不是 `N×R×R` 方阵，而是 `X[N,E]` 上三角 edge 向量，满足 `E=R(R-1)/2`。

原始全列的 1-based atlas 区间：

| atlas | 原始全列区间 | 理论 ROI |
|---|---:|---:|
| AAL116 | 1–116 | 116 |
| HO112 | 117–228 | 112（cortical 96 + subcortical 16） |
| CC200 | 229–428 | 200 |
| Dosenbach160 | 1409–1568 | 160 |

### 4.2 服务器

```text
服务器总根：/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain
服务器项目：/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825
服务器 SSL：/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/SSL/data
服务器 Python：/opt/conda/envs/fchn313/bin/python
```

用户最后报告已经正确配置 Python 3.13 环境，但对话中出现过 `3.13.9` 与用户文字 `3.13.19` 两个 patch 号。新会话必须以服务器实际命令输出为准并记录，不能擅自声称 patch 完全一致：

```bash
PY=/opt/conda/envs/fchn313/bin/python
"$PY" --version
"$PY" -m pip --version
"$PY" - <<'PY'
import numpy, pandas, scipy, sklearn, yaml, nibabel, openpyxl
print('numpy', numpy.__version__)
print('pandas', pandas.__version__)
print('scipy', scipy.__version__)
print('sklearn', sklearn.__version__)
print('PyYAML', yaml.__version__)
print('nibabel', nibabel.__version__)
print('openpyxl', openpyxl.__version__)
PY
```

本机曾通过门禁的主要版本为：Python 3.13.9、NumPy 2.3.5、Pandas 2.3.3、SciPy 1.16.3、scikit-learn 1.7.2、PyYAML 6.0.3、NiBabel 5.4.2、openpyxl 3.1.5。服务器运行期间应固定环境，并保存：

```bash
mkdir -p outputs/stage_c
"$PY" -m pip freeze > outputs/stage_c/server_environment_freeze.txt
"$PY" --version > outputs/stage_c/server_python_version.txt 2>&1
```

## 5. 阶段 A：已完成的最终输入准备

### 5.1 核心方案

Stage A 一次准备全部 `5 dataset × 4 atlas = 20` 个独立 cell，不分 protocol、不拟合模型。每个 cell 完成：

1. 权威 atlas 元数据和理论 ROI/edge universe；
2. subjects 与人口学精确回连及来源行 hash；
3. ROI/edge 稳定身份与结构性可用性；
4. 被试自身 QC；
5. 五数据集共同 ROI/edge 小型身份空间；
6. cell/stage manifest、hash、staging、archive 和 `_SUCCESS`。

稳定身份规则：

```text
roi_uid  = <atlas_lower>:roiNNN
edge_uid = <atlas_lower>:roiNNN--roiMMM
```

原始身份换算：

```text
roi_original_index      = roi_local_idx_0based + 1
global_original_column  = atlas_global_start_1based + roi_local_idx_0based
matrix_position         = 当前 FC 的 0-based 行/列位置；结构性缺失时为空
```

### 5.2 Atlas 权威来源

- AAL116、CC200、Dosenbach160 使用与 DPARSF/DPABI 列顺序一致的 DPABI 固定 tree：`8d4b16e46be65cce93662e34e1cfa22bd40d98dc`，许可证 `LGPL-2.1`。
- HO112 使用父项目已核验的 DPABI YCG cortical/subcortical NIfTI、MAT、XLSX 和 labels 快照。
- 来源快照位于 `config/atlas_metadata/sources/`，逐文件 SHA-256 记录在 source manifest。
- CC200 当前权威快照没有可靠解剖名称表，因此 `roi_name/description/network/hemisphere` 相关不可得字段按事实留空；`roi_label` 使用 parcel value，坐标由 NIfTI 质心计算。绝对不能编造脑区名。
- AAL hemisphere 依据官方 label 后缀：`_L→L`、`_R→R`、无侧别后缀→`M`。

### 5.3 Subjects 与人口学事实

- ADHD：844/844 精确匹配；label 规则 `DX>0 → ADHD=1`。官方 `ADHD-200_PhenotypicKey.pdf` 后续确认 raw Gender `0=female,1=male`。Stage A 保留 raw；Stage C 才映射 canonical `male=0,female=1`。有 1 个官方源 sex 缺失，不得填补。
- ABIDE：1085/1085，`SUB_ID` 必须补足/保持 7 位，`label=2-DX_GROUP`。
- ABIDE2：1027/1027，phenotype 必须以 `latin1` 读取，`label=2-DX_GROUP`。
- MDD：2347/2347 与 workbook 精确匹配，sheet `MDD→1`、`Controls→0`，sex `1=male,2=female`。早期“age 缺 3、sex 缺 1”结论已被当前工作簿复核证伪，绝对不要据此制造缺失。
- FCP：858/858 按 `<Site>_<Subject ID>` 精确匹配；sex `m/M→1`、`f/F→2`；176 个 mean FD 缺失必须保持为空，严禁从 realignment 参数回填。

### 5.4 QC

只依据单个被试自身 FC：

```text
总体 nonfinite edge 比例 >= 5%  → fail
任一 ROI incident nonfinite edge 比例 >= 30% → fail
```

ROI incident 分母是该结构性可用 ROI 的 incident edge 数，不是“edge 数 × 被试数”。这个分母 bug 曾被发现并修复。QC 是 atlas-specific，某被试一个 atlas fail 不得扩展到其他 atlas。

### 5.5 输出

每个 cell：`outputs/cells/<dataset>/<atlas>/`

| 文件 | 形式/尺寸 | 含义 |
|---|---|---|
| `subjects.csv` | `N×15` CSV | identity、site、label、人口学、来源和行 hash |
| `roi_manifest.csv` | 理论 `R×22` CSV | 完整 ROI metadata、结构状态和 matrix position |
| `edge_manifest.csv` | `R(R-1)/2×11` CSV | 稳定 edge 与两个 ROI 端点、结构状态和位置 |
| `fc_source.json` | 小 JSON | 源 FC 路径、shape、dtype、ROI 顺序、hash；不复制 FC |
| `ts_inventory.json` | 小 JSON | 非 `X_concat` 数组的 shape/dtype 描述，不是 TS 副本 |
| `nonfinite_mask.npz` | bool `N×E` | 源 FC 非有限位置 |
| `qc_subject.csv` | `N×13` CSV | 每名被试 QC 指标、最差 ROI UID、reason 和 hash |
| `cell_manifest.json` | JSON | 当前 cell source/artifact/identity/QC hash |
| `_SUCCESS` | 文本 | `stage_a_cell_ready` |

阶段级：`outputs/stage_a/`

- `common_roi_<atlas>.csv`：AAL 111、HO 111、Dosenbach 154、CC200 199 行；
- `common_edge_<atlas>.csv`：AAL 6105、HO 6105、Dosenbach 11781、CC200 19701 行；
- `input_inventory.csv`：20 行；
- `atlas_metadata_sources.csv`：4 行；
- `stage_a_summary.md`；
- `stage_a_manifest.json`；
- `_SUCCESS=stage_a_ready`。

### 5.6 20 个 cell 的正式结果

| dataset | atlas | N | available ROI | available edge | QC pass | QC fail |
|---|---|---:|---:|---:|---:|---:|
| adhd | HO112 | 844 | 112 | 6216 | 843 | 1 |
| adhd | AAL116 | 844 | 115 | 6555 | 839 | 5 |
| adhd | Dosenbach160 | 844 | 159 | 12561 | 837 | 7 |
| adhd | CC200 | 844 | 200 | 19900 | 840 | 4 |
| abide | HO112 | 1085 | 112 | 6216 | 1077 | 8 |
| abide | AAL116 | 1085 | 112 | 6216 | 1079 | 6 |
| abide | Dosenbach160 | 1085 | 155 | 11935 | 1077 | 8 |
| abide | CC200 | 1085 | 199 | 19701 | 1076 | 9 |
| abide2 | HO112 | 1027 | 111 | 6105 | 1022 | 5 |
| abide2 | AAL116 | 1027 | 113 | 6328 | 1023 | 4 |
| abide2 | Dosenbach160 | 1027 | 158 | 12403 | 1017 | 10 |
| abide2 | CC200 | 1027 | 200 | 19900 | 1016 | 11 |
| mdd | HO112 | 2347 | 112 | 6216 | 2344 | 3 |
| mdd | AAL116 | 2347 | 114 | 6441 | 2335 | 12 |
| mdd | Dosenbach160 | 2347 | 159 | 12561 | 2333 | 14 |
| mdd | CC200 | 2347 | 200 | 19900 | 2341 | 6 |
| fcp | HO112 | 858 | 112 | 6216 | 858 | 0 |
| fcp | AAL116 | 858 | 113 | 6328 | 852 | 6 |
| fcp | Dosenbach160 | 858 | 155 | 11935 | 850 | 8 |
| fcp | CC200 | 858 | 200 | 19900 | 851 | 7 |

MDD XLSX 读取时的 openpyxl 警告 `Unknown extension is not supported and will be removed` 在本流程中不构成失败：脚本只读取实际单元格、不保存或覆盖原 workbook，2347/2347 identity 和值均通过门禁。不要为了消除警告去修改源 XLSX。

### 5.7 Stage A 脚本与命令

```text
config/stage_a.yaml                 resolved 配置
config/stage_a.schema.json          配置门禁
src/fchn_stage_a_metadata.py        atlas metadata 纯函数
src/fchn_stage_a.py                 identity/subjects/QC/cell 纯函数
scripts/00_prepare_atlas_metadata.py metadata CLI
scripts/01_prepare_final_inputs.py   Stage A 主 CLI
tests/test_stage_a.py                单元与真实输入合同
tests/test_stage_a_gates.py          正式发布门禁
```

本机重建命令（已有正式输出时只在明确需要重建且接受 archive 后使用 `--replace-existing`）：

```powershell
Set-Location 'C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825'
$env:PYTHONDONTWRITEBYTECODE='1'
& 'C:\Users\CQQ\anaconda3\python.exe' scripts\00_prepare_atlas_metadata.py
& 'C:\Users\CQQ\anaconda3\python.exe' scripts\01_prepare_final_inputs.py --replace-existing
& 'C:\Users\CQQ\anaconda3\python.exe' -m pytest tests\test_stage_a.py tests\test_stage_a_gates.py -q -p no:cacheprovider
```

## 6. 阶段 B：已完成的 split plan

### 6.1 核心方案

Stage B 只生成 split，不打开 FC、不拟合模型。组合数：

```text
4 atlas × 4 disease dataset × 2 protocol = 32 groups
disease dataset = adhd, abide, abide2, mdd
protocol = LOSO, pooled10_site_label
FCP 不进入疾病 split
```

- LOSO：每个 site 依次作为 outer-test；
- pooled10_site_label：每个 `site×label` 内稳定排序，round-robin 分配到 10 folds；
- inner：请求 5 折 `site×label` round-robin；
- 每个实际 inner-train 必须两类且每类至少 10；
- 不合格时仅允许一次 2 折 label-stratified fallback；
- 仍不合格标记 `invalid_untrainable`，绝不训练常数分类器。

固定 seed 为 `20260625`，来源为父项目 `configs/fchn_latefusion.toml`。

### 6.2 Strict nested lineage

Stage B 不只保存 outer/inner，还保存 `nested_tasks.csv` 六层父子血缘：

```text
outer
base_oof
base_c_tuning
meta_oof
meta_base_oof
meta_base_c_tuning
```

标准 5-fold 情况下，每个 outer 对应 186 个 nested rows。Stage C 必须按 `parent_task_id` 和 partition 字段重建合法成员，不能重新随机划分。

### 6.3 输出与正式结果

每个 group：`outputs/splits/<atlas>/<dataset>/<protocol>/`

| 文件 | 含义 |
|---|---|
| `tasks.csv` | outer/inner task、role 计数、policy/status/reason、seed 和 split hash |
| `split_membership.csv` | task-role-sample 成员；不含人口学、label 或 FC |
| `nested_tasks.csv` | 六级 strict nested lineage、partition 和 identity hash |
| `split_manifest.json` | Stage A/config/source/artifact hash |
| `_SUCCESS` | `stage_b_split_ready` |

阶段级：`outputs/stage_b/split_inventory.csv`、`stage_b_summary.md`、`stage_b_manifest.json`、`_SUCCESS=stage_b_ready`。

正式聚合：

```text
groups                              32
outer tasks                        428
inner tasks                       2140
nested tasks                     79608
fallback outer tasks                 0
invalid_untrainable tasks            0
nested invalid_untrainable tasks     0
```

### 6.4 重大身份修复

ABIDE 的 Stage A `sample_key/subject_id` 是 7 位带前导零字符串。旧 pandas 默认推断曾把它写成整数/5 位；旧门禁也以默认 dtype 读取两侧，形成“共同错误仍相等”的盲区。最终修复：Stage B 和 Stage C 所有 `sample_key/subject_id` CSV 入口显式 `dtype=str`，门禁比较原始词法身份。

严禁在 Stage C 通过去前导零、转整数或模糊匹配“兼容”旧 split；这会掩盖上游身份损坏。Stage B 已按最终代码原子重建 32/32 groups，统计不变。

### 6.5 Stage B 脚本与命令

```text
config/stage_b.yaml
config/stage_b.schema.json
src/fchn_stage_b.py
scripts/02_prepare_split_plans.py
tests/test_stage_b.py
tests/test_stage_b_gates.py
```

```powershell
Set-Location 'C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825'
$env:PYTHONDONTWRITEBYTECODE='1'
& 'C:\Users\CQQ\anaconda3\python.exe' scripts\02_prepare_split_plans.py --replace-existing
& 'C:\Users\CQQ\anaconda3\python.exe' -m pytest tests\test_stage_b.py tests\test_stage_b_gates.py -q -p no:cacheprovider
```

## 7. 阶段 C：方法、实现、真实门禁与当前状态

### 7.1 科学闭环

对每个 `atlas × disease dataset × protocol × outer fold`：

```text
读取 Stage B 固定 outer/nested 成员
  → 当前训练边界内拟合 imputer/Ridge/scaler/PCA/KMeans/variance/Logistic
  → Base C-tuning 与一次性 top-level Base OOF
  → strict Meta OOF 与 Platt OOF
  → 完整 outer-train 重拟合三个 Base view
  → outer-test 只 transform/predict
  → 原子发布 fold artifact/manifest/_SUCCESS
```

只有同一 cell 的确定性 common-edge 列切片可以在一个 outer fold 内缓存；任何估计量和概率结果不得跨训练边界缓存。

### 7.2 三个 Base view 与 Meta

1. FC：edgewise train median → Ridge 残差化 → StandardScaler → PCA max 0 → L2 Logistic。
2. HOFC：FC edge 还原对称矩阵 → ROI profile 行中心化/归一化 → profile correlation 上三角 → train-only scaler/PCA max 128 → Logistic。
3. Normative：共同 edge 空间内，使用 `FCP HC + 当前训练边界疾病 HC`；KMeans/局部方差/收缩产生逐 edge z、10 个 summary，再训练 normative Logistic。FCP 不进入疾病 label 训练。

冻结参数：

```text
Ridge alpha=1.0，带截距
Base C=[0.003,0.01,0.03,0.1,0.3,1,3]
Meta C=[0.03,0.1,0.3,1,3]
selection=pooled log-loss，平局取更小 C
solver=liblinear，max_iter=5000，class_weight=None
HOFC PCA max=128
normative residual PCA max=32
K_eff=min(8,max(1,floor(H/10)))
H<10 → normative invalid
KMeans n_init=10
cluster n<3 → 局部方差回退总体方差
variance ddof=0，floor=1e-6，shrinkage=0.10
FCP site-aware health-only cross-fit=5 folds
tuning_workers=2；外层并行时 BLAS threads=1
```

Meta 固定 13 维：3 个 Base logit + 10 个 normative summary。prototype ID 只作 fold 内诊断，绝不能进入 Meta。最终 Meta 训练必须使用 top-level Base OOF，使每个有效 outer-train sample 恰好出现一次；不能纵向拼接 5 个 meta-train 导致重复。

Platt calibrator 只能拟合 strict Meta-OOF logit，再应用到 outer-test。

### 7.3 协变量和缺失策略

- Discriminative FC/HOFC：`age, age², canonical sex, mean_fd, log(scan_length)`；
- Normative common：`age, age², canonical sex, log(scan_length)`，不含 mean FD；
- `missing_covariate_policy=exclude_with_reason`。

缺失所需协变量时，不填补、不删除身份、不改变行序。只对全有限行预测，原位置回填；不合格的四个 view 行写：

```text
task_status=ineligible
reason_code=MISSING_REQUIRED_COVARIATE
prediction/logit/probability 留空
```

### 7.4 每个 fold 的输出

位置：`outputs/stage_c/folds/<atlas>/<dataset>/<protocol>/<outer_task_id>/`

| 文件 | 形式/尺寸 | 含义 |
|---|---|---|
| `predictions.csv` | `(4×N_test)×columns` | 每个 test sample 的 `fc/hofc/normative/full_fchn` 四行、真值快照、预测、C、状态和边界 hash |
| `normative_scores.npz` | 非 object NPZ | `sample_key[N_test]`、`edge_uid[common_E]`、`summary[N_test,10]`、`edge_z[N_test,common_E]`、nearest prototype |
| `model_state.npz` | 非 object NPZ | 三视图 imputer/Ridge/scaler/PCA/Logistic、anchor、Meta、Platt 的数值状态 |
| `fcp_crossfit_audit.csv` | FCP 每 sample 一行 | cross-fit fold、参考计数、fit/holdout identity hash、self-excluded、score-finite |
| `fold_manifest.json` | JSON | run/input/artifact hash、任务、C、shape、state keys |
| `_SUCCESS` | 文本 | `stage_c_fold_ready` |

NPZ 必须 `allow_pickle=false` 回读。字符串轴必须是固定宽度 Unicode，不能由 pandas `.to_numpy()` 产生 object dtype。`sample_key` 和 `edge_uid` 两条轴都必须存在，否则结果无法稳定回连。

### 7.5 已修复的 Stage C 问题

1. FCP canonical sex 被再次按 raw 1/2 映射，产生 NaN：改为单次映射并加测试。
2. normative `edge_z` 曾误接入 Meta，形成约 6000 维输入：改为严格 3 logits + 10 summaries = 13 维。
3. 最终 Meta 曾重复使用 5 个 meta-train：改为 top-level Base OOF 每 sample 一行。
4. FC/HOFC 资格筛选后 Meta label 未同步按 sample_key 对齐，出现 474/475：改为三视图唯一 identity 交集后回连权威 label。
5. outer003 缺失 canonical sex 使 Meta 接收 NaN：按 `exclude_with_reason` 保留身份，只预测有限行。
6. ABIDE 前导零被 pandas 吞掉：Stage B/C CSV 入口全部强制 string dtype，禁止模糊补救。
7. NPZ identity 轴曾是 object：改为显式 Unicode 并以 `allow_pickle=false` 门禁。
8. resume 曾可能只看 `_SUCCESS`：现在 marker、fold manifest 和当前 config/runner/src/A/B hash 必须全部相等才跳过。

### 7.6 真实实体门禁结果

- 常规 `HO112/adhd/LOSO/outer000`：`N_test=245`，predictions 980 行，`summary=(245,10)`，`edge_z=(245,6105)`，FCP audit 858 行。
- 缺失协变量 `HO112/adhd/LOSO/outer003`：`N_test=257`，predictions 1028 行；1024 行 passed、4 行 ineligible，四行属于同一个缺失 canonical sex 的 test identity；身份保留、未填补、未错位。
- ABIDE `HO112/abide/LOSO/outer000`：`N_test=37`，predictions 148 行，`summary=(37,10)`，`edge_z=(37,6105)`，strict Meta train=1040，7 位身份精确保持。

服务器重新运行 `tests/test_stage_c_pilot_gate.py` 后，用户报告 `3 passed`。这证明三类关键实体边界可正确执行，但不能证明其余 425 folds 或阶段级发布已经完成。

### 7.7 当前本机状态

本机最后核验：

```text
fold markers=15/428
staging=0
stage_c_manifest.json 不存在
outputs/stage_c/_SUCCESS 不存在
Stage C 本机进程已按用户要求终止
本机定时监控 automation c 已取消
```

本机两次停止均无 traceback、stderr 为 0，无法证明是算法错误；但也不得因此宣称 Stage C 完成。历史日志/结果已归档，包括：

```text
outputs/archive/stage_c_pre_missing_covariate_fix_20260829/
outputs/archive/stage_c_pre_subject_identity_fix_20260830T0229/
outputs/archive/stage_c_external_stop_20260831T085224/
outputs/archive/stage_c_pilot_*_20260829/
```

不要恢复本机串行运行。HO112 观测约 26–35 分钟/折、约 1.9 折/小时，剩余数百折约需 220 小时且高维 atlas 可能更慢；服务器应按互斥 outer fold 并行。当前实现是 scikit-learn CPU 流程，GPU 没有直接收益。

### 7.8 当前服务器状态：已确认与待确认

已确认：

- Python 3.13 conda 环境已纠正，不再使用项目 `.venv`；
- Stage A 门禁全绿，20/20 cell 计数与本机一致；
- Stage B 已生成并可进入 Stage C；
- 三个 Stage C 实体 fold 已生成，pilot gate `3 passed`。

必须现场确认：

- `scripts/run_stage_c_parallel_server.sh` 是否已存在；
- `outputs/stage_c/server_driver.pid` 是否存在且 PID 存活；
- 428-fold 并行任务是否实际启动；
- 当前 marker 数、staging 数、非空 stderr/traceback 数；
- `stage_c_manifest.json` 和 `_SUCCESS` 是否已经发布。

现有上下文不能支持“服务器全量正在运行”这一断言。新会话不得凭本文重复启动第二个 launcher。

## 8. 阶段 D：下一阶段计划（未实现）

Stage D 只能在 Stage C 全部门禁完成后实现。输入是 428 个 fresh fold 的：

- `predictions.csv`；
- `normative_scores.npz`；
- `model_state.npz`；
- `fold_manifest.json`；
- Stage C `stage_c_manifest.json` 与 `_SUCCESS`。

计划任务：

1. 对每个 `atlas×dataset×protocol×variant` 汇总 pooled、site-level macro、worst-site；
2. 报告 AUC、ACC、BACC、sensitivity、specificity、log-loss、Brier、ECE；
3. 单类别 held-out site/fold 的 AUC 记 NA，并保存 NA reason，不丢弃其其他指标；
4. 按站点配对 bootstrap CI、经验 p 值、有效站点数；
5. 汇总 calibration；
6. 以 held-out/cross-fitted `edge_z` 汇总 edge/ROI 选择频率、方向一致性、有效 fold/site 分母和跨站点复现；
7. 汇总个体连续异常评分，例如 `NCDP_RMS=sqrt(mean(z_edge²))`，以健康 OOF mid-rank 转百分位；
8. 生成 Stage D manifest、hash、summary、实施说明和 `_SUCCESS`。

Stage D 开发必须先写 schema/门禁与失败测试，再做最小实现。不得根据 Stage C 中间表现修改 QC、split、模型参数或 biomarker 规则。

## 9. 阶段 E：最终审计与发布计划（未实现）

Stage E 对全 run 做统一 round-trip：

1. `(dataset_id,sample_key)` 身份唯一性和原始词法保持；
2. ROI/edge 原始编号、全局列、端点和 matrix position；
3. NPZ 行列身份与 manifest 顺序；
4. train/validation/test/FCP fit boundary；
5. OOF 覆盖恰好一次、无重复无泄漏；
6. 非有限值、ineligible reason、单类别 NA reason；
7. config/source/code/artifact/stage parent hash；
8. schema 和关键 shape；
9. archive/恢复边界；
10. 生成跨主机外部 checksum 清单（当前 `_SUCCESS` 不是密码学签名）；
11. 原子发布最终报告、manifest、checksum 与 `_SUCCESS`；
12. 写完整 Stage E 实施说明。

## 10. 极简服务器上传清单

服务器已有 `SSL/data` 时，干净重建 A/B/C 所需的最小运行文件为：

```text
FCHN_20260825/config/     # 包含 stage_a/b/c、schema、atlas 来源快照、ADHD PDF
FCHN_20260825/src/
FCHN_20260825/scripts/
FCHN_20260825/tests/
CHyCR_20260621/configs/fchn_latefusion.toml
```

推荐额外上传但不参与模型运行：

```text
FCHN_20260825/doc/HANDOFF20260831.md
FCHN_20260825/doc/FCHN_HANDOFF_MANUAL_20260825.md
FCHN_20260825/doc/PHASE_A_IMPLEMENTATION_20260825.md
FCHN_20260825/doc/PHASE_B_IMPLEMENTATION_20260826.md
FCHN_20260825/doc/PHASE_C_IMPLEMENTATION_20260826.md
FCHN_20260825/outputs/stage_a/input_inventory.csv  # 可另存为 reference/input_inventory_windows.csv 做源 hash 对照
```

不应上传到服务器主 run：本机 `outputs/stage_c/folds`、本机 stage C 日志、本机 archive、跨 dataset/atlas 合并数据。服务器应使用自身 Linux 路径从原始 SSL 原生重建 A/B，并生成 server-native run hash。

若使用 SSH/SCP，主机名未在本会话提供，不能猜测。下面只是路径准确的模板，把 `<SERVER_HOST>` 替换为实际 SSH 主机；也可用已有文件上传界面完成同样目录映射：

```powershell
$LocalProject = 'C:\XY\FedTopo-Brain\CHyCR_20260621\FCHN_20260825'
$RemoteProject = '/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825'

ssh 'root@<SERVER_HOST>' "mkdir -p '$RemoteProject/configs'"
scp -r "$LocalProject\config"  "root@<SERVER_HOST>:$RemoteProject/"
scp -r "$LocalProject\src"     "root@<SERVER_HOST>:$RemoteProject/"
scp -r "$LocalProject\scripts" "root@<SERVER_HOST>:$RemoteProject/"
scp -r "$LocalProject\tests"   "root@<SERVER_HOST>:$RemoteProject/"
scp 'C:\XY\FedTopo-Brain\CHyCR_20260621\configs\fchn_latefusion.toml' "root@<SERVER_HOST>:$RemoteProject/configs/"
```

远端应先创建 `$RemoteProject/configs`。重复上传前先比较 hash；不要覆盖服务器正在使用且已有 fresh folds 对应的 runner/config。若确需更新，必须先停止 workers、归档旧 folds/logs，再重建受影响阶段。

Linux 上必须把三个 YAML 中的 Windows 路径改为服务器路径：

```bash
PROJ=/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825
SSL=/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/SSL

cd "$PROJ"
sed -i "s|^project_root:.*|project_root: $PROJ|" config/stage_a.yaml config/stage_b.yaml config/stage_c.yaml
sed -i "s|^data_root:.*|data_root: $SSL/data|" config/stage_a.yaml
sed -i "s|^seed_source:.*|seed_source: $PROJ/configs/fchn_latefusion.toml|" config/stage_b.yaml
```

路径修改会使服务器 config hash 与本机 config hash 不同，这是正常且必须的；因此不能把本机 fold 混入服务器 run。

## 11. 服务器从零重建 A/B 与实体门禁的命令

如果服务器 A/B 已通过，不要重复重建；先读 `_SUCCESS` 和 manifest。只有服务器项目是干净部署或现有 A/B 门禁失败时才执行：

```bash
PROJ=/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825
SSL=/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/SSL
PY=/opt/conda/envs/fchn313/bin/python

cd "$PROJ"
export PYTHONDONTWRITEBYTECODE=1
export OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1

"$PY" scripts/00_prepare_atlas_metadata.py --config config/stage_a.yaml
"$PY" scripts/01_prepare_final_inputs.py --config config/stage_a.yaml --data-root "$SSL/data"
"$PY" -m pytest tests/test_stage_a.py tests/test_stage_a_gates.py -q -p no:cacheprovider

"$PY" scripts/02_prepare_split_plans.py
"$PY" -m pytest tests/test_stage_b.py tests/test_stage_b_gates.py -q -p no:cacheprovider
```

若正式 A/B 输出已存在且经过核验确需重建，才分别加 `--replace-existing`；脚本会把旧结果整体归档。不要手工覆盖一部分 group/cell。

Stage C 单元与三个实体 fold：

```bash
"$PY" -m pytest tests/test_stage_c.py -q -p no:cacheprovider

"$PY" -u scripts/03_run_stage_c.py --pilot --atlas HO112 --dataset adhd --protocol LOSO \
  --outer-task HO112__adhd__LOSO__outer000__main
"$PY" -u scripts/03_run_stage_c.py --pilot --atlas HO112 --dataset adhd --protocol LOSO \
  --outer-task HO112__adhd__LOSO__outer003__main
"$PY" -u scripts/03_run_stage_c.py --pilot --atlas HO112 --dataset abide --protocol LOSO \
  --outer-task HO112__abide__LOSO__outer000__main

"$PY" -m pytest tests/test_stage_c_pilot_gate.py -q -p no:cacheprovider
```

这些 fold 可能各运行几十分钟。Shell 的 `wait "$PID"` 在子进程结束前没有输出是正常行为；用第二个 SSH 会话查看 `ps` 和相应日志，不要因 `wait` 静默就强杀。

## 12. 服务器 428 folds 的并行执行方案

### 12.1 并行原则

- 只并行互不重叠的单个 outer task；
- 每个任务使用 `--pilot --outer-task <exact_task_id>`；
- 不同时启动多个 `--all`；
- 每个 fold 内 `tuning_workers=2`，BLAS thread 固定 1；
- 8 CPU/32 GB 建议 `PARALLEL_FOLDS=4`；16 CPU/64 GB 建议 8；更大机器先从 8 开始，观察内存后最多 12；
- 全部单-fold worker 结束后，最后只运行一次 `--all`，它跳过 exact-fresh fold并发布 Stage C manifest/_SUCCESS。

### 12.2 推荐 launcher

先确认服务器不存在同名运行进程。若 `scripts/run_stage_c_parallel_server.sh` 不存在，在项目内创建以下文件并赋权；该文件目前**未在本机会话中确认已保存到服务器**：

```bash
#!/usr/bin/env bash
set -euo pipefail

PROJ=/root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825
PY=/opt/conda/envs/fchn313/bin/python
PARALLEL_FOLDS=${PARALLEL_FOLDS:-4}

cd "$PROJ"
export PYTHONDONTWRITEBYTECODE=1
export OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1
mkdir -p outputs/stage_c/logs

TASK_FILE=outputs/stage_c/server_outer_tasks.tsv
"$PY" - <<'PY'
from pathlib import Path
import pandas as pd

root = Path.cwd()
rows = []
for path in sorted((root / 'outputs' / 'splits').glob('*/*/*/tasks.csv')):
    atlas, dataset, protocol = path.parts[-4:-1]
    table = pd.read_csv(path, keep_default_na=False, dtype={'task_id': str})
    for task_id in table.loc[table['task_level'] == 'outer', 'task_id']:
        rows.append((atlas, dataset, protocol, str(task_id)))
if len(rows) != 428:
    raise SystemExit(f'expected 428 outer tasks, found {len(rows)}')
with (root / 'outputs' / 'stage_c' / 'server_outer_tasks.tsv').open('w', encoding='utf-8', newline='') as f:
    for row in rows:
        f.write('\t'.join(row) + '\n')
print('outer_tasks=428')
PY

run_one() {
    local atlas=$1 dataset=$2 protocol=$3 task_id=$4
    "$PY" -u scripts/03_run_stage_c.py \
        --pilot --atlas "$atlas" --dataset "$dataset" --protocol "$protocol" \
        --outer-task "$task_id" \
        >>"outputs/stage_c/logs/${task_id}.out" \
        2>>"outputs/stage_c/logs/${task_id}.err"
}
export -f run_one
export PROJ PY

xargs -P "$PARALLEL_FOLDS" -n 4 bash -c 'run_one "$@"' _ < "$TASK_FILE"

# 单进程做全部 exact-fresh 核验并发布 stage manifest/_SUCCESS。
"$PY" -u scripts/03_run_stage_c.py --all

"$PY" - <<'PY'
import json
from pathlib import Path
p = Path('outputs/stage_c')
d = json.loads((p / 'stage_c_manifest.json').read_text(encoding='utf-8'))
marker = (p / '_SUCCESS').read_text(encoding='utf-8').strip()
print('status', d.get('status'))
print('expected', d.get('expected_outer_folds'))
print('ready', d.get('ready_outer_folds'))
print('success', marker)
if (
    d.get('status') != 'passed'
    or d.get('expected_outer_folds') != 428
    or d.get('ready_outer_folds') != 428
    or marker != 'stage_c_ready'
):
    raise SystemExit('Stage C final gate failed')
PY
```

启动：

```bash
cd "$PROJ"
chmod +x scripts/run_stage_c_parallel_server.sh
PARALLEL_FOLDS=4 nohup bash scripts/run_stage_c_parallel_server.sh \
  >outputs/stage_c/server_driver_stdout.log \
  2>outputs/stage_c/server_driver_stderr.log &
echo $! | tee outputs/stage_c/server_driver.pid
```

同一代码/config/A/B hash 下重启相同 launcher 是可恢复的：已完成 exact-fresh fold 会跳过。若任一既有 fold stale，runner 会拒绝覆盖；必须先将 stale fold 和旧日志整体移动到项目内 `outputs/archive/<明确原因+时间>/`，再重启。

## 13. 查看 Stage C 进度的命令与含义

```bash
cd /root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825

# 当前完成 marker 数；是进度初筛，不等于 exact-fresh 最终证明。
find outputs/stage_c/folds -type f -name _SUCCESS 2>/dev/null | wc -l

# 正在运行的 driver/worker/finalizer。
pgrep -af '03_run_stage_c.py|run_stage_c_parallel_server.sh' || true

# 正在写的 staging 数；运行中可能短暂 >0，进程停止后应为 0。
find outputs/stage_c/folds -type d -name '*.staging' 2>/dev/null | wc -l

# 非空单-fold stderr 文件数量；>0 必须逐个完整读取，不能只 grep 最后一行。
find outputs/stage_c/logs -type f -name '*.err' -size +0 2>/dev/null | wc -l

# 含 traceback 的错误日志数；>0 立即停止“正常运行”判断。
grep -RIl 'Traceback' outputs/stage_c/logs 2>/dev/null | wc -l

# driver 总体进度和错误。
tail -f outputs/stage_c/server_driver_stdout.log
tail -n 120 outputs/stage_c/server_driver_stderr.log

# 最近完成的任务，只显示任务身份和状态，不显示被试信息。
find outputs/stage_c/folds -type f -name _SUCCESS -printf '%T@ %h\n' 2>/dev/null \
  | sort -n | tail -n 10

# 最终阶段标志；运行未完时不存在是正确状态。
test -f outputs/stage_c/stage_c_manifest.json && \
  /opt/conda/envs/fchn313/bin/python - <<'PY'
import json
from pathlib import Path
p=Path('outputs/stage_c/stage_c_manifest.json')
d=json.loads(p.read_text(encoding='utf-8'))
print({k:d.get(k) for k in ('status','expected_outer_folds','ready_outer_folds')})
PY
test -f outputs/stage_c/_SUCCESS && cat outputs/stage_c/_SUCCESS
```

无需每 15 分钟轮询。建议低频、边界导向：新 protocol/dataset/atlas 首折、每 25 folds、traceback、进程停止或最终完成时检查。普通 fold 连续运行、stderr 无变化时保持安静。

## 14. 报错后的固定处理流程

1. 不猜测，完整读取 driver stderr 和具体 `.err` traceback。
2. 记录失败任务身份、运行阶段、输入 shape、状态和 hash；不得打印 sample 列表。
3. 用聚合计数比较至少一个正常 fold 与失败 fold：train/test 数、资格数、缺失协变量数、common edge 数、finite 数、状态分布。
4. 找到最早违反合同的位置，而不是只处理最末异常。
5. 先新增最小失败测试并运行，确认 RED。
6. 做不改变科学定义的最小修复，确认单元测试 GREEN。
7. 单独重跑真实失败 outer fold并执行实体回读门禁。
8. 运行 Stage B/C 联合回归；若 Stage B 或 identity 代码变化，重建 Stage B。
9. runner/config/Stage B hash 变化后，把全部 stale folds和旧日志移动到项目 `outputs/archive/`，保存恢复证据。
10. 从新的 exact-fresh 边界恢复并行全量。
11. 不得启动 Stage D，直到 428/428 和阶段级门禁完成。

## 15. 已踩过的坑：绝对不要再踩

1. **阶段映射错误**：atlas metadata、稳定 ROI/edge、共同空间、subjects、QC 都属于 Stage A；split 才是 Stage B；outer-fold 模型是 Stage C。
2. **巨大合并文件**：禁止合并 dataset/atlas FC。一个 cell 一个加载单元。
3. **删 ROI 后重编号**：缺失 ROI 必须保留原始编号、全局列、结构状态和空 matrix position；edge 必须保留稳定端点。
4. **按位置解释 edge**：所有解释必须通过 `edge_uid→roi_uid_i/j→master ROI metadata` 回连。
5. **编造 CC200 名称**：没有权威名称就留空。
6. **人口学猜测/填补**：ADHD 官方缺失 sex、FCP 176 个 mean FD 均保持缺失；不得用众数、中位数或其他文件擅自补全。
7. **ABIDE 前导零**：所有 ID 入口 `dtype=str`，禁止转整数、去零匹配、模糊后缀匹配。
8. **QC incident 分母**：按 incident edge 数，不乘被试数。
9. **Meta 泄漏/重复**：最终 Meta 用 top-level Base OOF 每 sample 一行；不能拼 5 个 meta-train。
10. **把 edge_z 喂给 Meta**：主 Meta 只有 13 维，不是数千维。
11. **缺失协变量直接送 Logistic**：保持身份，只预测有限行，ineligible 写明确 reason。
12. **FCP 自泄漏**：每个 FCP sample 不能参与自己的 prototype/variance；必须有 5-fold self-exclusion audit。
13. **NPZ object dtype**：字符串轴显式 Unicode，`allow_pickle=false` 回读。
14. **只看 `_SUCCESS` 恢复**：必须同时比较 fold manifest 和当前 run hashes。
15. **并行多个 `--all`**：绝对禁止。单-fold worker 并行，最后一次 `--all` finalizer。
16. **本机与服务器 fold 混用**：Linux config hash不同，服务器必须 native 重建 A/B/C。
17. **Conda 与 `.venv` 嵌套**：曾出现提示 `(fchn313) (.venv)`，`which python` 仍指项目 `.venv/bin/python`，实际 Python 3.10.8，导致 NumPy 2.3.5 无法安装。始终使用 `/opt/conda/envs/fchn313/bin/python` 的绝对路径。
18. **把 `wait PID` 静默当卡死**：`wait` 只有结束才返回；另开 SSH 看日志和进程。
19. **pilot gate `.FF` 误判代码错误**：`test_stage_c_pilot_gate.py` 有 3 个真实门禁，必须先生成 outer000 ADHD、outer003 ADHD、outer000 ABIDE。最初 `.FF` 是两个先决 fold 不存在，不是方法错误。
20. **openpyxl 警告过度处理**：本项目不保存源 workbook，真实值和门禁通过；不要修改源 XLSX来“消警告”。
21. **修改 hash 后沿用旧 fold**：所有旧 fold 必须归档并从 fresh 边界重算。
22. **看到若干 folds 成功就宣称脚本全对**：成功只覆盖已走到的边界；完整阶段还需 428/428、manifest、hash、`_SUCCESS` 和联合验证。
23. **环境未纳入 run hash**：当前代码不会自动把 Python/package 版本纳入 run hash；必须保存 `pip freeze`，运行期间冻结环境。
24. **中间结果驱动改方案**：不得因中间 AUC、速度或个别 fold 修改 QC、C 网格、KMeans、protocol 或 biomarker 定义。

## 16. 当前本机核心文件 hash 快照

以下是 2026-08-31 本机文件 SHA-256，用于确认上传的源码是否来自最终修复版本。服务器三个 YAML 改为 Linux 路径后，其 config hash应不同；源码/脚本若未改动则应相同。

```text
aa027246e94f401768414efe25c34ace8f7e441b9dd748f94a4bd06d4a7e2cea  config/stage_a.yaml
abdc6e57701d6ac324c4290cad892383606cfce56c611e26093a983f9c090390  config/stage_b.yaml
7c66f94a1e15354820de33f637535018af99c92da568af28d59314f198adecc3  config/stage_c.yaml
6fdac9832758ac36a471c5380121d15748373d06fa41738d1d8de334b3f6e7fa  scripts/01_prepare_final_inputs.py
f1b8c9376f9a73fb15b8652fb18acce7c64a0ce1be7e9540c34c4243ef466794  scripts/02_prepare_split_plans.py
7068bcf515265c534179149240007e58156916aed6b38e4d6849dfbdc9a11765  scripts/03_run_stage_c.py
563eac2ccfc35530471e397eff482c11e4f3dde6c3f51b788793f342a6f3f1e9  src/fchn_stage_a.py
7915bc4c9bd01f0d1cdb518d6910a45c882a7266051cbdc1038e6653c394a9ab  src/fchn_stage_b.py
7e5cbd517ba92b28df8e4b4236a70b38916ce55653bb051a7fba6e02bc383f82  src/fchn_stage_c.py
185f8a2aa4f0eadcf42790f87cf629e11bec01df1dfcbfba177df22b0b0e0859  outputs/stage_a/stage_a_manifest.json
f1a5412474809a7720ca0e9819d385a958b8f71597d09b4d55b48a12a9297f15  outputs/stage_b/stage_b_manifest.json
```

不要把本文 hash 当成最终服务器 run hash；服务器路径解析、A/B 原生重建和任何后续合规修复都会产生新的受审计 hash。

## 17. Stage C 完成后的验收与移交到 Stage D

服务器 workers 全部完成后：

```bash
cd /root/siton-data-00092eadd25b4883b4b42798b0a1786b/Project/FedTopo-Brain/CHyCR_20260621/FCHN_20260825
PY=/opt/conda/envs/fchn313/bin/python
export PYTHONDONTWRITEBYTECODE=1
export OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1

# 只允许一个 finalizer。
"$PY" -u scripts/03_run_stage_c.py --all

find outputs/stage_c/folds -type f -name _SUCCESS | wc -l
cat outputs/stage_c/_SUCCESS
"$PY" -m pytest tests -q -p no:cacheprovider \
  --basetemp=outputs/stage_c/verification_tmp \
  --junitxml=outputs/stage_c/abc_verification_report.xml
```

随后机器核验：

```text
fold marker count = 428
stage_c_manifest.status = passed
manifest expected = 428
manifest ready = 428
每个 fold manifest hash 与当前实体一致
所有 run hash 指向当前 config/runner/src/Stage A/Stage B
outputs/stage_c/_SUCCESS = stage_c_ready
staging = 0
pytest = 0 failure
```

还必须更新 `doc/PHASE_C_IMPLEMENTATION_20260826.md`：替换过时 PID/进度，写入服务器环境、并行度、起止时间、428 聚合计数、stage manifest hash、联合验证结果、所有新增 launcher/log/env 文件说明和 C→D 字段接口。只有这份说明与实体门禁都完成，才把 `task_plan.md` 的 Stage C 标记为 complete，并开始 Stage D 的 TDD 设计。

## 18. 给新会话的最终原则

最短正确路径是：

```text
先核验服务器现场
  → 若正在正常运行，只低频观察关键边界
  → 若未启动且无错误，启动互斥 outer-fold 并行 launcher
  → 若报错，按 traceback + TDD 做最小修复并归档 stale
  → 428/428 后单进程 --all finalization
  → A/B/C 联合门禁与 Stage C 实施说明更新
  → 才设计和实现 Stage D
  → 最后 Stage E 全局审计发布
```

不要为了“快”缩小 C 网格、改变 KMeans、减少 nested 任务或混用旧 folds。当前真正的加速边界是服务器上互斥 outer-fold 的 CPU 并行；科学定义已经冻结。
